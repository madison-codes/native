import React, { Component } from 'react';
import { View } from 'react-native';

export class Main extends Component {
  state = {
    horizonalIsOn: false,
  };

  render() {
    return(
      <View />
    )
  }
}
